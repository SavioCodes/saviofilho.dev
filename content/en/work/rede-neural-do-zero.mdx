---
title: "Neural Network From Scratch"
summary: "An educational neural network implementation with deterministic evaluation, logs, tests, and a reproducible pipeline."
order: 3
year: "2026"
role: "Solo builder"
status: "Public repo, educational but well-scoped"
highlight: "This stays public because it shows engineering discipline inside a study project: reproducible evaluation, smoke checks, and structured docs."
kind: "public"
stack:
  - "Python"
  - "NumPy"
  - "pytest"
  - "JSONL logs"
repo: "https://github.com/SavioCodes/rede-neural-do-zero"
---

## Context

Study repositories only help a portfolio when they show craft, not just curiosity. I kept this one because it moved beyond a notebook exercise into a small, testable system with deterministic evaluation and report artifacts.

## Problem

I wanted to understand the mechanics of a neural network without hiding behind framework abstractions. At the same time, I wanted the repo to prove that learning projects can still honor engineering standards.

## Constraints

- No high-level ML framework.
- Reproducible results for CI and portfolio demos.
- Simple artifacts that can be inspected later.

<SignalGrid>
  <SignalCard
    label="Core proof"
    value="Deterministic evaluation script"
  />
  <SignalCard
    label="Repository signal"
    value="Logs and smoke checks in CI"
  />
  <SignalCard
    label="Operational concern"
    value="Portable JSON and JSONL artifacts"
  />
</SignalGrid>

## Architecture

```text
dataset utils
  -> normalization and split
  -> model
  -> backprop training
  -> evaluation metrics
  -> JSON / JSONL logs
```

<SystemDiagram
  title="Reproducibility route"
  subtitle="Data prep, training, deterministic evaluation, and plain-file artifacts"
  footer="The educational value stays visible because the system keeps the pipeline small, testable, and rerunnable."
>
  <FlowStep
    step="01"
    title="Normalize a tiny dataset"
    detail="The data utilities keep preprocessing readable and deterministic so the repo can be rerun the same way in CI and local demos."
  />
  <FlowStep
    step="02"
    title="Train without framework shortcuts"
    detail="Forward pass and backprop stay visible in code, which is the educational point of the repository."
  />
  <FlowStep
    step="03"
    title="Evaluate with a fixed seed"
    detail="The evaluation script locks parameters and seed so portfolio screenshots and CI outputs are reproducible."
  />
  <FlowStep
    step="04"
    title="Store artifacts in plain files"
    detail="JSON and JSONL logs make the result surface portable, inspectable, and easy to discuss without heavier tracking infrastructure."
  />
</SystemDiagram>

## Decisions and trade-offs

- I used framework-free implementation because the learning value was the point.
- I added a deterministic evaluation script because reproducibility is what separates a study repo from a throwaway experiment.
- I kept logs in JSON and JSONL rather than adding a heavier experiment tracker too early.

## What worked

- The repo now communicates rigor instead of just enthusiasm.
- The evaluation script provides an easy conversation path in interviews.
- The CI-oriented smoke evaluation is stronger than a basic `pytest -q` alone.

## What is still incomplete

- A small benchmark report or confusion matrix export would strengthen the artifact story further.
- There is room for richer experiment configuration without losing the repo's educational focus.

## Evidence

<TerminalFrame title="Evaluation flow" subtitle="Neural Network From Scratch repository">
{`$ pytest -q
$ python scripts/evaluate.py --seed 42 --epochs 400 --samples 240`}
</TerminalFrame>

```text
Outputs:
- logs/eval-summary.json
- logs/eval-history.jsonl
```
